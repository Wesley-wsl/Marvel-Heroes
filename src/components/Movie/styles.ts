import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        marginRight: 18,
        borderRadius: 10,
    },
    image: {
        width: 140,
        height: 230,
        borderRadius: 10,
    },
});
